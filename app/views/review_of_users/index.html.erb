<h1>Listing review_of_users</h1>

<table>
  <tr>
    <th>Poster</th>
    <th>User</th>
    <th>Content</th>
    <th>Points</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @review_of_users.each do |review_of_user| %>
  <tr>
    <td><%= review_of_user.poster_id %></td>
    <td><%= review_of_user.user_id %></td>
    <td><%= review_of_user.content %></td>
    <td><%= review_of_user.points %></td>
    <td><%= link_to 'Show', review_of_user %></td>
    <td><%= link_to 'Edit', edit_review_of_user_path(review_of_user) %></td>
    <td><%= link_to 'Destroy', review_of_user, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Review of user', new_review_of_user_path %>
