<%= form_for(@review_of_dish) do |f| %>
  <% if @review_of_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@review_of_dish.errors.count, "error") %> prohibited this review_of_dish from being saved:</h2>

      <ul>
      <% @review_of_dish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dish_id %><br />
    <%= f.text_field :dish_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :points %><br />
    <%= f.text_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
